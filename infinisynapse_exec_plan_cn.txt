小红书「种草翻车」检测 · 执行清单（中文版）
================================================
更正后方案：纯粘贴输入 · 自建知识库作 data_source · 先用自己 Key 跑通 /v1/query · SSO 作 D2 后段可选
赛程：截止 7/31 23:59 提交，评审 8/1–8/11 仍采集数据（我推断是约剩 2 天）
    【修正总览】实际落地已偏离"先跑通 /v1/query"：真实 API 是 SSE 异步驱动 IS 自带 agent（非 /v1/query 同步）；知识库供 agent 检索（SSE 模式下无 data_source 字段）；部署用 OnRender 常驻 Node（非 Vercel Serverless）。最新进度与 API 事实见 PROJECT_HANDOFF.md。

图例
  [WB] WorkBuddy = 你的 AI 助手/开发环境（写代码、读文档、出报告，不计比赛分）
  [IS] InfiniSynapse = 比赛平台（被集成 + 裁判，真实用户调用决定你 60% 分）
  专家中文名：软件架构师 / 后端·平台工程师 / 前端工程师 / 产品经理 / UX 研究员 / 行业场景研究员 / 法务顾问

------------------------------------------------
准备阶段（D0 · 今晚，约 1–2 小时）
------------------------------------------------
[ ] S0.1 开账号拿钥匙
    在哪：IS
    专家：后端/平台工程师
    完成：在 InfiniSynapse 控制台注册账号、创建应用、拿到 API Key（Bearer Token）。这是后面所有调用的凭证。

[ ] S0.2 把官方文档钉死
    在哪：WB
    专家：软件架构师
    完成：用 anysearch/WebAccess 把 Server API Reference、快速集成示例、SSO Guide 拉到本地，确认 /v1/query 的请求体（query + data_source）和返回结构。避免 D1 现查。
    【修正】真实 API 不是 /v1/query 同步，而是 SSE 异步：开 GET /api/ai/events?connId= 长连接 + POST /api/ai/message（body {type:"newTask",text,connId}）驱动 IS 自带 agent，结果从 SSE 流推送。详见 PROJECT_HANDOFF.md 第三节。
    【原因】实测 api.infinisynapse.cn/v1/query 返回 404（大赛示例端点已失效），官方 Server API Reference 只有异步 SSE 一套。原假设基于过时示例，必须更正，否则后端无法联通。

[ ] S0.3 敲定 6 维度评分体系
    在哪：WB
    专家：行业场景研究员 + UX 研究员
    完成：把「绝对化 / 功效夸张 / 软广 / 证据缺失 / 利益未披露 / 情绪裹挟」的权重、严重度分级（轻0.5/中1/重1.5）、证据句模板、prompt 草稿定稿。这是评审「严谨度」的抓手。
    【修正】维度是 7 个（非 6 个）：①商业标识缺失0.22 ②利益未披露0.18 ③绝对化用语0.16 ④隐性营销意图0.14 ⑤功效夸张0.12 ⑥信息可信度0.10 ⑦情绪裹挟0.08（和=1.00）。权重表见 PROJECT_HANDOFF.md 第六节。
    【原因】S0.3 定稿时把"软广"拆成了"商业标识缺失"和"隐性营销意图"两个独立维度，并补了"信息可信度"，故从 6 扩到 7。原清单写 6 维是早期草稿，需对齐定稿版。
    【待迭代·用户 7/30 补充】两项暗广增强信号已加进知识库（不新开维度，避免打乱权重和=1.00）：①文中 tag 命中品牌全称+产品名/品类组合 → 上调「商业标识缺失/利益未披露」置信度；②探店类带地点/POI 标签+点名商户且无利益声明 → 上调「利益未披露」置信度并联动增强维度⑨发布者行为。详见 infinisynapse_kb_content.md 第 1.4 节 + 第 3.1 证据句。知识库 md 已更新，需重传控制台覆盖旧文件才生效。

------------------------------------------------
D1 · 跑通最小可用（MVP 骨架）
------------------------------------------------
[ ] S1.1 建评分知识库（作 data_source）
    在哪：IS
    专家：后端/平台工程师
    完成：在 InfiniSynapse 建一个 RAG 知识库/数据源，装入「广告法禁用词 + 软广话术样本」，作为 /v1/query 的 data_source。解决无用户库问题。
    【修正】知识库已建好并启用：知识库名 Ad_risk_scoring_dimensions（ID bbdcaa3b-7881-4470-a4e2-8c75af6dcb95，enabled=1）+ 数据源 xhs_kb_file，已双向绑定。SSE 模式下无 data_source 字段，靠 server.js 的 prompt 引导 agent 去检索该知识库。
    【原因】原以为靠 /v1/query 的 data_source 字段挂载，实测无此字段；agentic 模式下知识库挂载靠 prompt 引导 + 控制台"启用"开关，已实测确认知识库处于启用状态。

[ ] S1.2 写转发函数（持有 Key）
    在哪：WB
    专家：后端/平台工程师
    完成：写一个 Serverless 函数（持有 Key），接收前端笔记文本，转发 POST /v1/query，返回 JSON。Key 只在这层，绝不进前端。
    【修正】已完成 server.js（非 Serverless，是常驻 Node 服务）：接收前端 /api/analyze → 开 SSE 长连接 → 发 newTask 让 IS agent 分析 → 收流提取最终 Markdown 报告返回。Key 仅 process.env.IS_API_KEY。已删旧版 api/analyze.js/dev-server.js。
    【原因】①真实端点是 SSE 异步非 /v1/query，写法完全不同；②agentic 任务跑 30s~3min，Vercel Serverless 短超时（默认~10s/Pro 60s）扛不住，必须用 OnRender 常驻 Node（start=node server.js）。所以不是 Serverless 函数，是常驻服务。

[✅] S1.3 写前端页面（纯粘贴输入）
    在哪：WB
    专家：前端工程师
    完成：单页应用——「粘贴正文」输入框 + 调转发函数 + 骨架屏/分析中态。不做链接抓取（按更正）。
    【修正】已完成（7/30）：`public/index.html` = 粘贴框 + 调 /api/analyze + marked.js 渲染 result；首屏显示「## 摘要」、点"查看评分依据"折叠「## 评分依据」7 维明细；固定免责条。后端 server.js 同步改 prompt 为三段式（## 摘要/## 评分依据/## 给普通用户的提醒）并新增同源静态托管 public/index.html（免 CORS）。已本地验证：首页可服务、无 Key 时返回友好 NO_API_KEY。
    【原因】选了方向 A（顺 IS 自带 agent 的 agentic 模式），agent 直接输出 Markdown 报告而非结构化 JSON，故前端渲染 Markdown 而非画分数表格，省去反复调 prompt 逼它出 JSON 的时间（比赛仅剩约 1.5 天）。
    【待迭代·用户 7/30 提出】用户体验上「粘贴链接」比「粘贴正文」更有效，等本版（粘正文）跑通部署后，再迭代支持粘链接：需新增「链接抓取正文」环节（小红书笔记为 JS 动态渲染，普通 fetch 抓不到，要专门抓取+处理反爬），抓到正文后再走现有分析流程。本版先不做，避免拖慢进度与引入新坑。

[~] S1.4 联调验证（调试，不计分）
    在哪：WB（调试流量）
    专家：后端/平台工程师
    完成：验证后端→IS 链路的返回结构、单次耗时、data_source 是否生效。
    ⚠️ 这一步是 WB 调 IS，算你自己的调试流量，不计入比赛 60% 分。
    【修正】已跳过本地 PowerShell 联调（实测 PowerShell 发中文 body 变乱码 + 偶发 502，低收益高成本），改为「前端写好 + 部署 OnRender 后，用真浏览器 fetch 测一次」一气呵成（浏览器 fetch 自动 UTF-8，无中文坑）。
    【原因】本地 PowerShell 测 ≠ 真实部署环境，且中文编码坑浪费时间；真实路径是浏览器 fetch→后端→IS，UTF-8 自动正确。后端架构已用真实 Key 实测跑通（73s 返回 ok:True，结构正确）。
    【当前状态】尚未执行。将在 S1.6 部署完成后，用真浏览器访问公网地址验证。

[~] S1.5 端到端跑通 1 篇出分
    在哪：WB
    专家：前端 + 后端
    完成：粘贴一篇笔记 → 出结果，证明完整链路通。
    【修正】端到端目标改为：粘贴一篇笔记 → agent 返回「风险等级（低/中/高）+ 一句话结论 + 命中维度 + 提醒」的 Markdown 报告，证明链路通。不再强求出 0–100 数字分。
    【原因】方向 A 下 agent 输出定性报告更稳；0–100 分需方向 B 强约束 prompt 才有，时间紧先跑通定性版，有余力再升级。
    【当前状态】尚未执行。将与 S1.4 一起在 S1.6 部署完成后，在真浏览器里粘贴第一篇笔记验证。

[ ] S1.6 部署上线拿公网 URL（含 S1.4/S1.5 验证）
    在哪：WB（第三方托管 Vercel/OnRender）
    专家：前端工程师
    完成：把代码部署到 Vercel/OnRender，得到公网可访问地址（提交硬门槛 + 真实用户入口）。
    【修正】部署锁定 OnRender（常驻 Node，start=node server.js，环境变量配 IS_API_KEY），不用 Vercel。
    【原因】Vercel Serverless 默认超时太短（Hobby 10s / Pro 60s），扛不住 agentic 任务 30s~3min；OnRender 常驻服务无此限制，且满足"公网部署"硬门槛。

------------------------------------------------
D2 · 维度渲染 + 分享 + 合规（含可选 SSO）
------------------------------------------------
[ ] S2.1 结果渲染 + 合规措辞
    在哪：WB
    专家：前端工程师 + 法务顾问
    完成：前端把分数拆成 6 维度条 + 证据句高亮 + 改写建议；一律用「疑似营销话术」「可信度待你判断」，加「AI 生成、仅供参考、非官方认定」免责条（法务硬约束）。
    【修正】维度条改为 7 维（对齐 S0.3 定稿权重），其余合规措辞不变。
    【原因】同 S0.3，维度已从 6 扩到 7，渲染需一致。

[ ] S2.2 分享卡片生成
    在哪：WB
    专家：前端工程师
    完成：用 html-to-image 生成结论卡片，自动隐去原文只留结论，带应用链接。传播钩子。

[ ] S2.3 隐私 / 关于弹层
    在哪：WB
    专家：法务顾问 + 前端工程师
    完成：声明「文本仅用于本次分析、不持久化、与小红书官方无关」，满足合规 + 提交材料。

[ ] S2.4 预置 demo 笔记
    在哪：WB
    专家：产品经理 + UX 研究员
    完成：自写 3 篇脱敏笔记（翻车/正常/边界），首页一键测，降低首体验门槛。

[ ] S2.5 申请 SSO 凭证（可选）
    在哪：IS（拉新开关）
    专家：后端/平台工程师
    完成：在 InfiniSynapse 申请 clientId/clientSecret、填回调域名白名单。接了，新用户通过你的应用登录才计入你 60% 分。

[ ] S2.6 SSO 回调处理（可选）
    在哪：WB
    专家：后端/平台工程师
    完成：写 /auth/infini/callback 服务端，用一次性 code 换用户信息。

⚠️ 权衡：S2.5/2.6 与「2 天出 MVP」直接冲突。建议 MVP 先用自己 Key 跑真实调用（满足准入），SSO 作 D2 后段有余力再接。冲一等奖才必须接 SSO。

------------------------------------------------
D3 · 打磨 + 提交 + 运营
------------------------------------------------
[ ] S3.1 UI 打磨
    在哪：WB
    专家：前端工程师
    完成：移动端适配、错误降级提示、加载体验优化。

[ ] S3.2 写 API 集成说明
    在哪：WB
    专家：后端/平台工程师 + 软件架构师
    完成：一页说明「调了 /v1/query、data_source 用自建知识库、Key 在服务端」——提交硬门槛材料。
    【修正】集成说明改为「调 InfiniSynapse 自带 agent（SSE 异步：/api/ai/events + /api/ai/message），后端 server.js 持有 Key 转发，知识库 Ad_risk_scoring_dimensions 已启用供 agent 检索」。
    【原因】原 /v1/query 端点不存在，说明文档必须对齐真实 API，否则评审/复现会失败。

[ ] S3.3 比赛提交
    在哪：IS
    专家：产品经理
    完成：在 InfiniSynapse×CSDN 大赛页提交：应用介绍 + 上线地址 + API 集成说明 + 截图。

[ ] S3.4 提交后运营拉新到 8/11
    在哪：WB + IS
    专家：产品经理
    完成：发 3 条模板（小红书/朋友圈/社群）带应用链接持续拉新；在 IS 后台看活跃量。
    ⚠️ 提交不是终点，8/1–8/11 仍采集数据。

------------------------------------------------
一句话红线
    WB 帮你调 IS 的流量只是调试，不计入比赛成绩；真实分数靠
    公网应用 + 真实用户调用 +（最好）接 SSO 拉新。
