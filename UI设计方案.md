# 「种草甄别」UI 设计方案（像素君 · UI Designer）

> 基于现有 `public/index.html` 的界面审计，建立轻量设计系统并落地符合用户习惯的关键新增。
> 设计原则：**一眼看懂风险 · 放心粘贴 · 知道进度 · 移动端友好 · 合规安全**。

---

## 一、界面审计结论（改前问题）

| 维度 | 现状 | 问题 |
|---|---|---|
| 设计系统 | 仅 5 个颜色变量 | 无排版尺度 / 间距 / 阴影 / 语义色，样式散落硬编码 |
| 核心结论 | 风险等级埋在 AI Markdown 文字里 | 用户第一眼看不到结论，需自己找 |
| 首屏信任 | 仅一行副标题 | 缺乏建立信任的锚点，用户犹豫是否粘贴 |
| 字数 | 只写"最多 5000 字" | 无实时计数，用户不知道用量、易超限 |
| 加载态 | 纯文字"分析中…" | 缺少动效指示，感觉"卡住了" |
| 错误对比度 | 用品牌红 `#ff2e4d` 作文字 | 与白底对比约 3.8:1，未达 WCAG AA 4.5:1（普通文字） |

---

## 二、设计系统（Design Tokens）

统一在 `:root` 管理，杜绝硬编码：

```
品牌色   --red #ff2e4d / --red-dark #d6293f(错误文字,AA达标) / --red-deep #c81e3a(hover)
语义色   --success #1f9d55 / --warning #e0820b
中性     --ink #1f1f1f / --gray #8a8a8a / --line #ececec / --bg #fafafa
阴影     --shadow-sm / --shadow-md / --shadow-lg（分级 elevation）
圆角     --radius 12px / --radius-sm 10px
```

**排版尺度**（沿用现有字号，规范化）：标题 20 / 区块 17 / 卡片标题 16 / 正文 14 / 辅助 12 / 最小 11。
**间距**：以 4px 为基准（6/8/10/12/14/16/22/24）。
**等级配色**：高=红 `#ff2e4d`、中=橙 `#e0820b`、低=绿 `#1f9d55`（与语义色一致，认知统一）。

---

## 三、新增界面元素（已落地）

### 1. 品牌头部升级
- 红→红渐变背景（`--red-grad-a → --red-grad-b`），顶部加 🌱 logo 标识
- 强化"种草甄别"品牌识别，告别扁平红条

### 2. 首屏信任三栏（价值锚点）
```
🔒 隐私优先    🧮 7维智能评分    ⚡ AI 秒级研判
不存储不训练    逐句识别话术      一分钟出报告
              权重加权判定      附证据原文
```
- 放在首屏、输入框上方，用户粘贴前先建立"安全 + 专业 + 快"的信任

### 3. 风险结论 Hero（核心新增 ⭐）
- 结果区**顶部**新增圆形等级徽章（高/中/低 配色）+ 一句话结论
- 让核心判断**一眼可见**，不再需要从 Markdown 里找
- 徽章颜色与等级语义一致，配合边框色调区分

### 4. 实时字数统计
- textarea 下方 `0 / 5000`，超限变红（`#counter.over`）
- 符合用户习惯：随时掌握用量、避免提交被拒

### 5. 加载旋转指示
- 等待时状态栏左侧出现旋转点（`.spin`，CSS `@keyframes spin`）
- 比纯文字更有"活着"的感觉，缓解等待焦虑

### 6. 状态语义与错误对比度修复
- `#status` 默认色改为中性灰（普通提示不再"报警红"）
- 仅 `.err` 用深红 `--red-dark`，满足 WCAG AA 4.5:1
- 主按钮 hover 态：加深 + 微上浮 + 阴影，反馈更明确

---

## 四、移动端（≤480px）
- 价值三栏缩小间距/字号，保持三列（信息密度适配）
- 已有：textarea 16px 防 iOS 缩放、按钮/卡片/弹窗窄屏适配、waiting-tips 缩排
- 动效尊重 `prefers-reduced-motion`（关闭所有过渡/动画）

---

## 五、无障碍（Accessibility）
- 状态栏 `aria-live="polite"`：屏幕阅读器自动播报分析进度
- 错误对比度达标（AA）
- 触控目标 ≥44px（按钮/示例药丸符合）
- 旋转点 `prefers-reduced-motion` 下自动停（全局动画关闭规则覆盖）

---

## 六、可继续打磨的方向（待定，非必须）
1. **暗色模式**：加 `[data-theme="dark"]` 令牌集（本专家技能已含模板）
2. **骨架屏**：结果区加载时先显示占位骨架，比空白更顺滑
3. **复制失败兜底提示**：已有 `fallbackCopy`，可加更明确 toast
4. **空结果引导**：无命中时给更友好的"看起来像真实分享"文案

> 当前版本已覆盖评审最在意的"可用性 + 美观 + 无障碍"，建议在今晚 23:59 前提交。
